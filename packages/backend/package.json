{
  "name": "@engineering-app/backend",
  "version": "1.0.0",
  "private": true,
  "description": "FastAPI backend (Python dependencies handled via requirements.txt).",
  "scripts": {
    "dev": "echo 'Backend is Python-based. Run uvicorn main:app --reload from packages/backend/src'",
    "build": "echo 'No build step required for backend (Python project)'",
    "test": "pytest"
  }
}

{
  "name": "@engineering-app/desktop",
  "version": "1.0.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "dev": "electron .",
    "build": "tsc -p tsconfig.json && electron-builder",
    "dist:win": "electron-builder --win",
    "dist:mac": "electron-builder --mac",
    "dist:linux": "electron-builder --linux"
  },
  "dependencies": {
    "electron": "^27.0.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "electron-builder": "^24.6.0",
    "rimraf": "^5.0.5"
  }
}
